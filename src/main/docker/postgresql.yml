version: '2'
services:
  eloco-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/eloco/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=eloco
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
